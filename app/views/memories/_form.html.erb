
  <%= simple_form_for memory do |f| %>
    <%= f.input :title, label: 'Titre' %>
    <%= f.input :histoire, label: 'Souvenir', :input_html => {:rows => 10} %>

    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.submit "Ajouter le souvenir" %>
  <% end %>
