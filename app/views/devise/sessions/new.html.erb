<%= render 'layouts/header.html.erb', title: 'Accès à ma mémoire', subheading: ""  %>

<div class="container">
  <div class="col-sm-5">
    <h2 class="text-center">Inscription</h1>
    <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => 'form-horizontal' }) do |f| %>
    <hr>
      <%= f.input :firstname, label: 'Prénom', error: 'Le prénom est obligatoire.' %>
      <%= f.input :lastname, label: 'Nom', error: 'Username is mandatory, please specify one' %>
      <%= f.input :email %>
      <%= f.input :password, label: 'Mot de passe' %>
      <%= f.input :password_confirmation, label: 'Confirmation du mot de passe' %>
      <%= f.button :submit, "Créer un compte"%>
    <% end %>
  </div>
  <div class="col-sm-5 col-sm-offset-2">
    <h2 class="text-center">Déjà Inscrit ?</h1>
    <%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => 'form-horizontal' }) do |f| %>
      <%= f.input :email %>
      <%= f.input :password, label: "Mot de passe"%>
      <%= f.button :submit, "Se connecter"%>
    <% end %>
    <br>
    <%= render "devise/shared/links" %>
  </div>
</div>
